DROP TABLE people IF EXISTS;

CREATE TABLE people  (
    id BIGINT IDENTITY NOT NULL PRIMARY KEY,
    jobTitle VARCHAR(100),
    emailAddress VARCHAR(200),
    firstName VARCHAR(50),
    lastName VARCHAR(50),
    salary VARCHAR(50),
    amoutAddToSalary VARCHAR(50),
    phoneNumber VARCHAR(50),
    
    
    
    
);
